function setCardId(button) {
        var ID = $(button).data("id");
        $('#DeleteIdInput').val(ID);
}

function setAddressId(button) {
        var ID = $(button).data("id");
        $('#DeleteAddressInput').val(ID);
}